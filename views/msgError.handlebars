<div class="h1 text-danger text-center fw-bold"> Erro! ALGO DEU ERRADO</div>
<hr>
<div class="container p-3">
    <div class="row p-3">
        <div class="col-8 offset-2 lead text-center display-6">
            {{msdDeErro}}
        </div>
        {{#if msdDeErro2}}
        <hr>
         {{msdDeErro2}}
        {{else}}
        {{/if}}
    </div>
</div>